
on:
  push:
    branches:
      - master

name: Deploy String boot to Amazon EC2

jobs:
  deploy:
    name: DEPLOY
    runs-on: ubuntu-18.04

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up JDK 1.8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
        shell: bash

      - name: Build with Gradle
        run: ./gradlew build
        shell: bash

      - name: Make zip file
        uses: montudor/action-zip@v0.1.0
        with:
          args: zip -qq -r ./$GITHUB_WORKFLOW.zip .

      - name: ENV TEST
        env:
          GITHUB_WORKFLOW: ${{ GITHUB_WORKFLOW }}
        run: echo $GITHUB_WORKFLOW
        shell: bash
        
#       - name: Upload artifact
#         uses: actions/upload-artifact@v1
#         with:
#           name: app
#           path: test.zip

      - name: Upload to S3 
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: aws s3 cp --region ap-northeast-2 ./$GITHUB_WORKFLOW.zip s3://isntyet-deploy/$GITHUB_WORKFLOW.zip
    


        
#run: aws s3 cp --recursive --region ap-northeast-2 ./test.zip s3://isntyet-deploy/test.zip
#      - name: Configure AWS credentials
#        uses: aws-actions/configure-aws-credentials@v1
#        with:
#          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#          aws-region: ${{ secrets.AWS_REGION }}
